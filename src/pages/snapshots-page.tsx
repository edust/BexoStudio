import { FrozenModulePage } from "@/pages/frozen-module-page";

export default function SnapshotsPage() {
  return (
    <FrozenModulePage
      description="原有快照、恢复预览和能力检查 UI 已冻结。当前阶段先不继续做恢复流程展示。"
      title="Snapshots"
    />
  );
}
