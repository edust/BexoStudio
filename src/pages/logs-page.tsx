import { FrozenModulePage } from "@/pages/frozen-module-page";

export default function LogsPage() {
  return (
    <FrozenModulePage
      description="原有恢复批次和日志诊断界面已冻结。当前阶段不继续展示执行数据，只保留空白工作页。"
      title="Logs"
    />
  );
}
