import { FrozenModulePage } from "@/pages/frozen-module-page";

export default function ProfilesPage() {
  return (
    <FrozenModulePage
      description="原有 Codex Profile 管理界面已冻结。当前只保留未来回归时可复用的路由位。"
      title="Profiles"
    />
  );
}
