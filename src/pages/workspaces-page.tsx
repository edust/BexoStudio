import { FrozenModulePage } from "@/pages/frozen-module-page";

export default function WorkspacesPage() {
  return (
    <FrozenModulePage
      description="原有工作区编排与项目编辑器已退出当前主流程。这个页面暂时只保留路由壳，等待新框架稳定后再决定是否回归。"
      title="Workspaces"
    />
  );
}
